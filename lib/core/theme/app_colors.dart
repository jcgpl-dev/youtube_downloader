import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF06080C);
  static const Color surface = Color(0xFF0B1017);
  static const Color surface2 = Color(0xFF111827);
  static const Color card = Color(0xFF131A24);

  // Borders & Dividers
  static const Color border = Color(0xFF1E293B);
  static const Color divider = Color(0xFF1A2230);

  // Primary Accent (YouTube-inspired Red)
  static const Color primary = Color(0xFFFF2D2D);
  static const Color primaryHover = Color(0xFFFF4242);
  static const Color primaryDark = Color(0xFFCC1E1E);

  // Secondary Accent
  static const Color secondary = Color(0xFF2A3342);

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB3B8C3);
  static const Color textMuted = Color(0xFF7C8596);

  // Status Colors
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Download Progress
  static const Color progressBackground = Color(0xFF1A2230);
  static const Color progressValue = primary;

  // Sidebar
  static const Color sidebar = Color(0xFF05070A);
  static const Color sidebarActive = Color(0xFF131A24);

  // Input Fields
  static const Color inputBackground = Color(0xFF0F141D);
  static const Color inputBorder = Color(0xFF243041);

  // Shadows & Glows
  static const Color shadow = Color(0x99000000);
  static const Color redGlow = Color(0x40FF2D2D);
}
